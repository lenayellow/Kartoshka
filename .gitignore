# Built application files
*.apk
*.aar
*.ap_
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/
release/

# Gradle build files and caches
.gradle/
build/
**/build/

# Local configuration (contains your machine-specific SDK path — never commit)
local.properties

# Proguard
proguard/

# Log files
*.log

# Android Studio / IntelliJ
.idea/
*.iml
*.ipr
*.iws
captures/
.navigation/

# Keystore files (NEVER commit these)
*.jks
*.keystore

# External native build folder
.externalNativeBuild/
.cxx/

# Google Services
google-services.json

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Claude Code local settings (keep CLAUDE.md, ignore local-only state)
.claude/

# Screenshots folder for prompts (optional — remove this line if you want to commit them)
# screenshots/

# Backend build artifacts
backend/api.exe
backend/api.exe~
backend/response.json

# Cloud Functions deploy artifacts (zip archives and compiled binaries)
backend/deploy/ycfunc/*.zip
backend/deploy/ycfunc/main
backend/deploy/ycfunc/ycfunc
backend/deploy/ycfunc/sa_key.json

# serverless.yaml with real secrets — use serverless.yaml (placeholders) in repo
backend/deploy/serverless.yaml.local

# Secrets — NEVER commit
backend/.env
backend/sa_key.json
*.env
!*.env.example
keystore.properties

# Local tooling / runtime distributions
python-*.exe
ydb-*/
*.exe